// built in data type number,string, boolean, undefine, null

let userName: string = "SHAHARIAR KOBIR SHAKIL";
let userId: number = 200001;
let isActive = true;

console.log(`Name: ${userName} and Id: ${userId} status: (${isActive})`);
